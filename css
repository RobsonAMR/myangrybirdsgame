/* Reset de estilos para garantir consistência em diferentes navegadores */
body, h1, p, button, ul, li {
    margin: 0;
    padding: 0;
}

body {
    background-color: #87CEEB; /* Cor de fundo do jogo */
    font-family: Arial, sans-serif;
    overflow: hidden; /* Impede a rolagem da página */
}

/* Estilo para o título do jogo */
h1 {
    text-align: center;
    font-size: 36px;
    color: #FF4500; /* Cor do título */
    margin-top: 20px;
}

/* Estilo para botões */
button {
    background-color: #FF4500; /* Cor de fundo dos botões */
    color: white; /* Cor do texto dos botões */
    border: none;
    padding: 10px 20px;
    font-size: 18px;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

button:hover {
    background-color: #FF6347; /* Cor de fundo dos botões ao passar o mouse */
}

/* Estilo para o container principal do jogo */
#game-container {
    width: 800px;
    height: 600px;
    background-image: url('background.jpg'); /* Imagem de fundo do jogo */
    background-size: cover;
    position: relative;
    margin: 0 auto;
    overflow: hidden;
}

/* Estilo para o pássaro do jogo */
#bird {
    width: 50px;
    height: 50px;
    background-image: url('bird.png'); /* Imagem do pássaro */
    background-size: cover;
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
}

/* Estilo para os porcos do jogo */
.pig {
    width: 50px;
    height: 50px;
    background-image: url('pig.png'); /* Imagem dos porcos */
    background-size: cover;
    position: absolute;
    /* Adicione as coordenadas (top e left) para posicionar os porcos no jogo */
}

/* Outros estilos específicos do jogo podem ser adicionados conforme necessário */
